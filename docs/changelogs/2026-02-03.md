# 2026-02-03

## Sammanfattning
- Säkerhetsfix: flyttade admin‑auth cookie token till miljövariabler (ingen hårdkodad hemlighet i repo).
- Nginx uppdaterad för cookie‑baserad admin‑auth och HTTPS‑routing i prod.
- Backend SSE‑auth accepterar admin‑cookie som alternativ till SSE‑token.
- SSE‑klient hämtar token asynkront före anslutning.
- Prod compose uppdaterad (n8n:stable, lokal portbindning, nya env‑variabler).

## Ändrade filer (urval)
- .env.example
- backend/main.py
- docker-compose.prod.yml
- fgt_dashboard/assets/sse-client.js
- nginx.conf

## Noteringar
- `nginx/htpasswd` ändrades lokalt men committades inte.
- SSL‑arbetsorder finns i `ARBETSORDER_SSL.md` (inte slutförd i detta steg).
